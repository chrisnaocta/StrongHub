import 'package:flutter/material.dart';

class NewsPageAdmin extends StatelessWidget {
  const NewsPageAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("Dashboard Admin (Kosong)", style: TextStyle(fontSize: 18)),
      ),
    );
  }
}
